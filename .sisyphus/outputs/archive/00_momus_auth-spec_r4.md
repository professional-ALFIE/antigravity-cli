**[REJECT]**
**Summary**: §7-3 is now aligned with §7-1/§7-2/§7-4, and the cited repo/reference files are present and relevant. However, the plan still has blocking verification gaps: the wake-up trigger is not actually locked, one of the previously flagged E2E scenarios remains non-executable, and a large remainder of the final verification matrix is still placeholder-level.

**Blocking Issues**
1. `.sisyphus/plans/00-plan-v0.2.1-auth-overhaul.md` Feature 4 / `E2E-4`: the wake-up integration point is still undefined. §7 defines the wake-up procedure, but it never states exactly when the product runs it, and `E2E-4` still says “message path auto-trigger or manual,” which leaves two incompatible execution paths. Lock one trigger contract and rewrite `E2E-4` with the exact command/path.
2. `.sisyphus/plans/00-plan-v0.2.1-auth-overhaul.md` `E2E-3`: the scenario still depends on “mock quota to 73% / 65%” without naming the tool or mechanism that performs that mock, so a developer cannot run the test as written. Specify the concrete harness (for example, mock server, fixture file, or test-only env override), the exact command to run it, and the observable output that proves the boundary crossing.
3. `.sisyphus/plans/00-plan-v0.2.1-auth-overhaul.md` remaining success criteria are still not executable QA scenarios for the final verification wave: `R-1`, `R-2`, `R-3`, `R-4`, `R-6`, `W-2`, `W-3`, `W-5`, `SS-1`, `SS-3`, and `NF-1`–`NF-4` still use placeholder proofs like “test,” “E2E,” or “measure with time” without tool + concrete steps + expected result. Expand each of those IDs to the same specificity now used in `L-1`, `A-2`, and `W-1`, otherwise the final verification wave cannot be executed.
