{
  "name": "antigravity-bridge-extension",
  "displayName": "Antigravity Bridge",
  "description": "Local HTTP server exposing antigravity-sdk for CLI access",
  "version": "0.1.2",
  "license": "AGPL-3.0-or-later",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {},
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test:auto-run": "node --import tsx --test test/auto-run.test.ts"
  },
  "dependencies": {
    "antigravity-sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "tsup": "^8.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.3.0"
  }
}
